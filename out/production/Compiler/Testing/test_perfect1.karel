class program {
    void foo ( ) {
        if ( front-is-clear ) {

        } else {
            iterate ( 5 ) {
                move ( )
            }
        }
    }

    void turnRight ( ) {
        turnLeft ( )
        turnLeft ( )
        turnLeft ( )
    }

    program ( ) {
        foo ( )
    }
}